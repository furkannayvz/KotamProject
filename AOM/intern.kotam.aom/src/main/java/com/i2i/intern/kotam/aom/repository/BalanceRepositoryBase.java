/*
package com.i2i.intern.kotam.aom.repository;

import com.i2i.intern.kotam.aom.model.Balance;
import java.util.Optional;

public interface BalanceRepositoryBase {
    Optional<Balance> getBalanceByMsisdn(String msisdn);
    boolean insertBalance(String msisdn, Long package_id , Long left_minutes , Long left_sms , Long left_data );
    boolean updateBalance(String msisdn, Balance updatedBalance);
    void updateDataBalance(String msisdn, Long data);
    void updateSmsBalance(String msisdn, Long sms);
    void updateMinutesBalance(String msisdn, Long minutes);
}

 */
